import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Movement Lab")
                .font(.largeTitle)
                .padding(.bottom, 8)
            Text("Basic iOS App Scaffolding")
                .foregroundColor(.secondary)
        }
    }
}
